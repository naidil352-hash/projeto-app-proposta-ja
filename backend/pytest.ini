[pytest]
markers =
    unit: tests that do not require MongoDB, auth, or live API infrastructure
    integration: tests that require MongoDB, auth, or live API infrastructure
    legacy_external: legacy tests that require explicit external infrastructure and are disabled by default
addopts = -ra
asyncio_mode = auto
